% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/func_helper.R
\name{s}
\alias{s}
\title{Construct a B-spline basis matrix}
\usage{
s(
  x,
  P = 3,
  K = min(7, max(3, floor(log2(length(unique(x)))))),
  limits = c(NA, NA),
  knots = "eq"
)
}
\arguments{
\item{x}{A numeric vector of predictor values.}

\item{P}{A non-negative integer giving the spline degree. \code{P = 3} corresponds to a cubic B-spline basis.}

\item{K}{An integer giving the number of basis functions to return. The default increases slowly with the number of unique values in \code{x}.}

\item{limits}{A numeric vector of length 2 giving the lower and upper boundary limits for the spline basis. Missing values are replaced by \code{min(x)} and \code{max(x)}.}

\item{knots}{Either a numeric vector of knot locations, or one of \code{"eq"} or \code{"quantile"}. If \code{"eq"}, knots are placed uniformly between \code{limits[1]} and \code{limits[2]}. If \code{"quantile"}, knots are placed at equally spaced empirical quantiles of \code{x}.}
}
\value{
A numeric matrix with one row per element of \code{x} and one column per spline basis function.
}
\description{
Build a B-spline basis for a numeric vector using a Cox-de Boor style recursion. By default, the function constructs a cubic spline basis (\code{P = 3}) and chooses the number of basis functions from the number of unique values in \code{x}.
}
\details{
Boundary limits are taken from \code{x} unless supplied explicitly. Knot locations may be given directly as a numeric vector, or generated either at equally spaced locations (\code{"eq"}) or at empirical quantiles (\code{"quantile"}).


The returned basis has \code{length(x)} rows and \code{k} columns.

When \code{knots} is generated internally, the function first creates \code{K - P + 1} knot locations and then augments them with repeated boundary knots so the recursion can be evaluated.
}
\examples{
x <- seq(0, 1, length.out = 10)

# Default cubic basis
B <- s(x)
dim(B)

# Equally spaced knots with custom basis size
B2 <- s(x, K = 5, knots = "eq")

# Quantile-based knots
B3 <- s(x, knots = "quantile")

}
